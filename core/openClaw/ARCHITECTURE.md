# 🦞 OpenClaw 架构与探索指南

> **环境**: macOS (Apple Silicon) · Node v22.22.0 (fnm, arm64) · OpenClaw v2026.2.12  
> **Agent**: Pi 🧊 · 简洁直接 · 中文为主  
> **最后更新**: 2026-02-13

---

## 一、系统全景

```
                    ┌──── 你的输入 ────┐
                    │                 │
              📱 Telegram        💻 TUI / WebChat
              (随时随地)          (本地调试)
                    │                 │
                    ▼                 ▼
┌─────────────────────────────────────────────────────┐
│              Gateway (ws://127.0.0.1:18789)         │
│              ─────────────────────────────          │
│  Session Manager · Cron Engine · Tool Router        │
│  Media Pipeline  · Hooks Engine                     │
│                                                     │
│  ┌───────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ Pi Agent  │  │  Skills  │  │  Hooks           │ │
│  │ (Claude   │  │ obsidian │  │ session-memory   │ │
│  │  Opus 4)  │  │ github   │  │ command-logger   │ │
│  │           │  │ summarize│  │                  │ │
│  └─────┬─────┘  │ nano-pdf │  └──────────────────┘ │
│        │        │ ...      │                        │
│        │        └──────────┘                        │
└────────┼────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│         LLM API (via OpenRouter)       │
│  ┌──────────────────────────────────┐  │
│  │ 🥇 Claude Opus 4     (主力)     │  │
│  │ 🥈 Gemini 2.5 Pro    (备选)     │  │
│  │ 🥉 DeepSeek Chat     (兜底)     │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

---

## 二、核心概念

### 2.1 Agent 身份系统

OpenClaw 的 Agent 不是无状态的聊天机器人——它有"灵魂"、"记忆"和"行为规范"。

```
~/.openclaw/workspace/             ← Agent 的"家"
├── AGENTS.md                      ← 行为规范：每次唤醒先读什么、安全边界、何时该说话
├── SOUL.md                        ← 灵魂：性格、价值观、交互风格
├── IDENTITY.md                    ← 身份卡：名字、emoji、形象描述
├── USER.md                        ← 你是谁：Agent 对你的了解
├── TOOLS.md                       ← 环境备忘：你的设备、SSH host、偏好设置
├── BOOTSTRAP.md                   ← 出生证明（首次对话后删除）
├── HEARTBEAT.md                   ← 心跳/状态
├── MEMORY.md                      ← 长期记忆（关键决策、教训、学到的东西）
├── memory/                        ← 日记（每天一个 YYYY-MM-DD.md）
│   ├── 2026-02-13.md
│   └── ...
└── .git/                          ← 整个 workspace 有 Git 版本控制
```

#### 每次 Session 的加载顺序

```
1. SOUL.md       → 我是谁？
2. USER.md       → 我在帮谁？
3. memory/今天.md → 最近发生了什么？
4. MEMORY.md     → 长期记得什么？（仅主会话加载）
```

> **关键洞察**：这些 `.md` 文件就是 AI 的"大脑"。直接编辑它们 = 改变 AI 的性格和记忆。

### 2.2 模型 Failover 策略

```jsonc
{
  "model": {
    "primary": "openrouter/anthropic/claude-opus-4",        // 主力
    "fallbacks": [
      "openrouter/google/gemini-2.5-pro",                    // 备选
      "openrouter/deepseek/deepseek-chat"                    // 兜底
    ]
  }
}
```

当主力模型不可用（rate limit / 网络问题）时，自动切换到下一个。

### 2.3 Skills 技能系统

Skills 是 OpenClaw 的"插件"，赋予 Agent 特定领域的能力。

| 已安装 Skill | 能力 | 用途 |
|-------------|------|------|
| `obsidian` | 读写 Obsidian 笔记库 | 知识管理 |
| `github` | 操作 GitHub（Issue, PR, Repo） | 代码协作 |
| `summarize` | 总结网页 / 文章 | 信息处理 |
| `nano-pdf` | 读取 PDF 内容 | 文档处理 |
| `blogwatcher` | 监控 RSS/博客更新 | 信息监控 |
| `gemini` | Google Gemini 专属能力 | 模型增强 |
| `model-usage` | 追踪模型使用量 | 费用监控 |
| `clawhub` | 浏览和安装社区 Skills | 扩展能力 |

#### 浏览更多 Skills

```bash
# 在 Telegram 里对 Pi 说
搜索 ClawHub 上有什么好用的 Skills

# 或者终端
openclaw skills search <关键词>
```

### 2.4 Hooks 事件钩子

Hooks 是 Agent 生命周期中的自动化行为。

| 已启用 Hook | 触发时机 | 作用 |
|------------|---------|------|
| `session-memory` | 每次对话结束 | 自动保存记忆到 `memory/` 目录 |
| `command-logger` | 每次执行命令 | 记录 Agent 执行了什么操作 |

#### 可选 Hooks

| Hook | 作用 | 何时启用 |
|------|------|---------|
| `boot-md` | 启动时注入自定义 Markdown 上下文 | 想要 Agent 每次启动都读特定内容时 |

---

## 三、文件系统架构

```
~/.openclaw/                              ← OpenClaw 主目录
├── openclaw.json                         ← 🔧 核心配置（模型、通道、认证）
├── workspace/                            ← 🧠 Agent 大脑（身份 + 记忆）
│   ├── AGENTS.md / SOUL.md / ...
│   └── memory/
├── state/                                ← 运行时状态
│   ├── sessions/                         ← 会话数据
│   └── logs/                             ← Gateway 日志
└── skills/                               ← 已安装的 Skills

~/Desktop/Projects/openClaw/              ← 📁 项目文档（你在这里）
├── ARCHITECTURE.md                       ← 本文件
├── SUMMARY.md                            ← 安装过程总结
└── update-key.sh                         ← API Key 更新脚本
```

---

## 四、当前配置一览

```jsonc
// ~/.openclaw/openclaw.json 核心字段
{
  "agents.defaults.model.primary":    "openrouter/anthropic/claude-opus-4",
  "agents.defaults.model.fallbacks":  ["gemini-2.5-pro", "deepseek-chat"],
  "channels.telegram.enabled":        true,
  "hooks.session-memory":             true,
  "hooks.command-logger":             true,
  "gateway.port":                     18789,
  "gateway.bind":                     "loopback"  // 仅本地访问
}
```

---

## 五、命令速查

### 终端命令

| 命令 | 说明 |
|------|------|
| `openclaw gateway status` | 查看 Gateway 状态 |
| `openclaw gateway restart` | 重启 Gateway |
| `openclaw dashboard` | 打开 Web 控制面板 |
| `openclaw doctor` | 诊断配置问题 |
| `openclaw tui` | 终端对话界面 |
| `openclaw update` | 更新 OpenClaw |
| `openclaw skills search <keyword>` | 搜索 Skills |
| `openclaw pairing approve telegram <code>` | 批准设备配对 |

### Telegram 聊天命令

| 命令 | 说明 |
|------|------|
| `/status` | 当前模型、token 用量 |
| `/new` | 重置对话（清空上下文） |
| `/compact` | 压缩上下文（对话太长时） |
| `/think high` | 深度思考模式（复杂问题用） |
| `/think low` | 快速回复模式（日常用） |
| `/model` | 查看 / 切换模型 |

---

## 六、探索路线图

### Phase 1 ✅ 已完成（今天）

- [x] 安装 OpenClaw + Gateway Daemon
- [x] 配置 OpenRouter（Claude Opus 4 + Failover）
- [x] 连接 Telegram Bot
- [x] 完成 Agent 人格设定（Pi 🧊）
- [x] 启用 session-memory + command-logger

### Phase 2: 深度定制（本周）

#### 🧠 定制 Agent 人格
```bash
# 直接编辑这些文件来调整 Pi 的行为
vim ~/.openclaw/workspace/SOUL.md       # 修改性格和交互风格
vim ~/.openclaw/workspace/USER.md       # 告诉 Pi 更多关于你的信息
vim ~/.openclaw/workspace/IDENTITY.md   # 完善名字、emoji 等
vim ~/.openclaw/workspace/TOOLS.md      # 添加你的环境信息
```

**探索方向**：
- [ ] 在 `USER.md` 中添加：你的技术栈偏好、工作习惯、常用项目路径
- [ ] 在 `TOOLS.md` 中添加：Obsidian 笔记库路径、SSH 配置、常用工具
- [ ] 在 `SOUL.md` 中调整：根据使用体验微调 Pi 的沟通风格

#### 📝 连接 Obsidian
- [ ] 配置 `obsidian-cli` 默认 Vault 路径
- [ ] 测试笔记创建、搜索、更新
- [ ] 让 Pi 帮你维护 `000 认知架构地图.md`

#### 🐙 连接 GitHub
- [ ] 配置 GitHub Personal Access Token
- [ ] 测试 Issue 创建、PR Review
- [ ] 让 Pi 监控你关注的仓库

### Phase 3: 自动化（本月）

#### ⏰ Cron 定时任务
```
# 在 Telegram 里对 Pi 说：
每天早上 9 点给我发一份今日待办和天气摘要
每周五下午 5 点提醒我写周报
```

**探索方向**：
- [ ] 每日摘要（天气 + 日历 + 提醒）
- [ ] RSS 监控（新文章自动推送到 Telegram）
- [ ] GitHub 通知整合

#### 🌐 浏览器自动化
OpenClaw 内置 Chromium，可以自动化浏览器操作：
```
# 对 Pi 说：
打开 Hacker News，找出今天 AI 相关的 top 5 文章
去 GitHub Trending 看看本周最火的 Python 项目
```

**探索方向**：
- [ ] 信息抓取与摘要
- [ ] 网页表单填写
- [ ] 竞品监控

### Phase 4: 高级架构（按需）

#### 🖥️ 远程部署（24/7 在线）
将 Gateway 部署到 VPS，笔记本关机也能用：
```
VPS (Linux)                    你的 Mac / 手机
├── OpenClaw Gateway     ←→    Telegram App
├── 24/7 运行                   随时随地发消息
└── Cron 任务不间断
```

**探索方向**：
- [ ] 在 VPS 上安装 OpenClaw
- [ ] 配置 Tailscale 远程访问
- [ ] 迁移 Gateway 到服务器

#### 🤖 多 Agent 架构
不同 Agent 负责不同领域：
```
Pi (Main)        → 日常助手：对话、思考、规划
Coder            → 编程专家：只讨论代码，不闲聊
Researcher       → 调研员：浏览器自动化 + 信息整合
```

**探索方向**：
- [ ] 学习 `AGENTS.md` 中的 Agent 路由配置
- [ ] 创建专用 Agent Profile
- [ ] 配置 Agent 间的任务分发

#### 🔧 自定义 Skills
当社区 Skills 满足不了你的需求时：
```
~/.openclaw/workspace/skills/
└── my-custom-skill/
    ├── SKILL.md         ← Skill 定义
    ├── index.js         ← 逻辑实现
    └── package.json
```

**探索方向**：
- [ ] 研究现有 Skill 的代码结构
- [ ] 为自己的特定需求写 Skill
- [ ] 发布到 ClawHub 社区

---

## 七、与 Antigravity 的协作模型

```
┌─────────────────────────────────────────┐
│          OpenClaw (Pi 🧊)               │
│          全局 AI 中枢                    │
│                                         │
│  ✦ Telegram 随时对话                     │
│  ✦ 跨项目视角（能读所有文件夹）            │
│  ✦ 适合：规划、思考、跨项目搜索            │
│  ✦ 24/7 在线（合盖后需 VPS）             │
│                                         │
│  "帮我对比 SmartSend 和                  │
│   rss-ai-digest 的技术栈"               │
└──────────────────┬──────────────────────┘
                   │
                   │ 互补
                   │
┌──────────────────┼──────────────────────┐
│          Antigravity                    │
│          深度编码搭档                    │
│                                         │
│  ✦ 编辑器内运行                          │
│  ✦ 深度理解当前项目结构                   │
│  ✦ 适合：写代码、调试、重构               │
│  ✦ 能直接编辑、运行、测试                 │
│                                         │
│  "重构这个组件的性能问题"                 │
└─────────────────────────────────────────┘
```

**原则**：Pi 是指挥部，Antigravity 是手术刀。

---

## 八、安全与成本

### 安全注意事项

| 项目 | 风险 | 对策 |
|------|------|------|
| API Key | 泄露 = 别人用你的钱 | 不要在聊天中发 Key；定期轮换 |
| SOUL.md | 被恶意修改 = Agent 行为异变 | workspace 有 Git，可以回滚 |
| 第三方 Skills | 可能有恶意代码 | 只装 ClawHub 高星 Skills，审查代码 |
| Gateway Token | 泄露 = 他人可控制你的 Agent | 保持 `bind: loopback` |

### 成本估算（OpenRouter）

| 模型 | 输入价格 | 输出价格 | 日均估算 |
|------|---------|---------|---------|
| Claude Opus 4 | $15/M tok | $75/M tok | ~$1-5/天（重度使用） |
| Gemini 2.5 Pro | $1.25/M tok | $10/M tok | ~$0.1-0.5/天 |
| DeepSeek Chat | $0.14/M tok | $0.28/M tok | ~$0.01-0.05/天 |

> 💡 **省钱技巧**：日常用 `/think low`，复杂问题切 `/think high`。或临时 `/model` 切到 Gemini。

---

## 九、常见问题

### Agent 不回复？
```bash
openclaw gateway status     # 检查 Gateway 是否在运行
openclaw doctor             # 诊断问题
openclaw gateway restart    # 重启
```

### 想重置 Agent 记忆？
```bash
# 清空日记
rm -rf ~/.openclaw/workspace/memory/*

# 重置长期记忆
echo "" > ~/.openclaw/workspace/MEMORY.md
```

### 想换模型？
```bash
# 方法 1: Telegram 里临时切换
/model

# 方法 2: 永久修改配置
vim ~/.openclaw/openclaw.json
# 改 agents.defaults.model.primary
openclaw gateway restart
```

### 更新 API Key？
```bash
./update-key.sh sk-or-v1-你的新Key
```

---

## 十、参考链接

| 资源 | 链接 |
|------|------|
| 官方文档 | [docs.openclaw.ai](https://docs.openclaw.ai) |
| GitHub | [github.com/nichochar/openclaw](https://github.com/nichochar/openclaw) |
| ClawHub (Skills 商店) | [clawhub.dev](https://clawhub.dev) |
| OpenRouter (模型 API) | [openrouter.ai](https://openrouter.ai) |
| 模型定价 | [openrouter.ai/models](https://openrouter.ai/models) |

---

*Created: 2026-02-13 · 随使用经验持续更新*
